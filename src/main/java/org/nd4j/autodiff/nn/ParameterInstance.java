package org.nd4j.autodiff.nn;

import org.nd4j.autodiff.samediff.SDVariable;

class ParameterInstance extends Parameter {
    ParameterInstance(SDVariable data) {
        super(data);
    }
}
